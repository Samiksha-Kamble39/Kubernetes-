apiVersion: autoscaling/V2
kind: HorizontalPodscalar
metadata: 
  name: my-app-Hpa
spec:
  scaleTargetRef:
    apiversion: apps/v1
    kind: Deployment
    name: my-app
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        avergaeUtilization: 60  
